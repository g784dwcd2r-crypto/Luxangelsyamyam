# Updated README

## Owner PIN Login Example
Change the owner PIN login example from 1234 to 0000.

## First-time Setup
Before running the project, ensure to execute the following command:

```bash
npm run setup
```

After the setup, you can start the development server using:

```bash
npm run dev
```